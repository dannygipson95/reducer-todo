export const initialState = [
{
    item: 'learn about reducers',
    completed: false,
    id: 1
}
]
export const reducer = (state, action)=>{
    switch(action.type){
        default:
        return state
    }
}